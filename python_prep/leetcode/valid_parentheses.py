""" 
Quick explanation of the leetcode problem practice: 

Problem: Valid Parentheses
Given a string s containing just the characters '(', ')', '{', }', '[' and ']', determine if the input string is valid.

An input string is valid if:

Open brackets must be closed by the same type of brackets.

Open brackets must be closed in the correct order.

Every close bracket has a corresponding open bracket of the same type.

Examples:

"()" → true

"()[]{}" → true

"(]" → false

"([)]" → false

"{[]}" → true

Your task: Write a Python function is_valid(s: str) -> bool that solves this.

 """


#instantiation of the function
def bool is_valid() {

#creating the tuple of accepted values (if user types in anything like it, it will be put through the sifting process)
 accepted_values_boundary = {"()", "()[]{}", "(]", "{[]}"};

 #creating the actual bool assignments for each 

 ""


}

int main() {






}